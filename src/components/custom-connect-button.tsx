import { ConnectButton } from '@mysten/dapp-kit';

export const CustomConnectButton = () => {
	return (
		<ConnectButton className='bg-primary! w-full text-white! hover:bg-primary/90! transition-colors!' />
	);
};
